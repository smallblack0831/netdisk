flowchart LR
    U[用户浏览器] --> FE[前端单页应用<br/>Vue3 + Vite + Vue Router + Element Plus]

    subgraph Frontend[前端层 netdisk-front]
      FE --> R1[页面模块<br/>Login / Main / Share / Recycle / Admin / AI / Preview]
      FE --> HTTP[统一请求层<br/>Axios封装 + 拦截器]
      HTTP -->|baseURL: /api| VP[Vite代理<br/>5173 -> 7090]
    end

    VP --> BE[后端服务<br/>Spring Boot /api]

    subgraph Backend[后端层 netdisk]
      BE --> C1[控制层 Controller<br/>AccountController<br/>FileInfoController<br/>ShareController<br/>WebShareController<br/>RecycleController<br/>AdminController<br/>AiController]
      C1 --> S1[业务层 Service<br/>UserInfoServiceImpl<br/>FileInfoServiceImpl<br/>FileShareServiceImpl<br/>EmailCodeServiceImpl<br/>AiServiceImpl]
      S1 --> M1[持久层 Mapper(MyBatis)<br/>UserInfoMapper<br/>FileInfoMapper<br/>FileShareMapper<br/>EmailCodeMapper<br/>AiChatHistoryMapper]
      S1 --> RC[RedisComponent / RedisUtils]
      S1 --> FS[本地文件存储<br/>temp分片目录 + file正式目录]
      C1 --> EX[AGlobalExceptionHandlerController<br/>统一异常处理]
    end

    M1 --> DB[(MySQL<br/>netdisk库)]
    RC --> RD[(Redis)]

    S1 --> GH[GitHub OAuth<br/>授权/令牌/用户信息]
    S1 --> MAIL[SMTP邮件服务<br/>邮箱验证码]
    S1 --> AI[DeepSeek API<br/>AI对话]
